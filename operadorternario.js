// Cliente: 100 = premium; >100 comum

let pontos = 127;
let tipo = pontos > 100 ? 'premium' : 'comum'
console.log(tipo);

// Se pontos for maior que 100 será do TIPO premium.
// Caso contrário, será do TIPO comum.

